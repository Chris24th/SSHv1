import 'package:flutter/material.dart';

class SmokePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
        child: Center(
      child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
        Text('Smoke chart Page'),
      ]),
    ));
  }
}
